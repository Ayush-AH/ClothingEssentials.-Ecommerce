<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet" />
</head>
<body>
    <div class="w-full px-44 py-10">
        <div class="flex items-center justify-between mb-6">
            <h1 class="text-4xl font-semibold">Update Product Info</h1>
            <a class="text-blue-600 text-lg outline-none" href="/admin">Admin Panel <i class="ri-arrow-go-back-line"></i></a>
        </div>
        <form class="flex flex-col " action="/createProduct" method="post" enctype="multipart/form-data">
            <input type="file" name="productImage">
            <h4 class="font-semibold mt-4">Product Name:</h4>
            <input class="border border-black/50 rounded w-[30%] py-1 px-3" type="text" name="productName" placeholder="Product Name...">
            <h4 class="font-semibold mt-4">Product Price:</h4>
            <input class="border border-black/50 rounded w-[30%] py-1 px-3" type="text" name="price" placeholder="Product Price">
            <h4 class="font-semibold mt-4">Discount:</h4>
            <input class="border border-black/50 rounded w-[30%] py-1 px-3" type="text" name="discount" placeholder="Discount">
            <h4 class="font-semibold mt-4">Collection:</h4>
           <div class="px-2 py-1 border border-black/50 w-[30%] rounded">
            <select class="w-[100%] outline-none" name="collection" >
                <option value="winter collection">Winter Collection</option>
                <option value="summer collection">Summer Collection</option>
            </select>
           </div>
           <h4 class="font-semibold mt-4">Category:</h4>
           <div class="px-2 py-1 border border-black/50 w-[30%] rounded">
            <select class="w-[100%] outline-none" name="category" >
                <option value="t-shirt">T-shirt</option>
                <option value="track pants">Track Pants</option>
            </select>
           </div>
           <h4 class="font-semibold mt-4">Availability:</h4>
           <div class="px-2 py-1 border border-black/50 w-[30%] rounded">
            <select class="w-[100%] outline-none" name=" availability" >
                <option value="true">Available</option>
                <option value="false">Unavailable</option>
            </select>
           </div>
           <input class="bg-orange-500 px-8 py-1 text-white rounded w-fit mt-6" type="submit" value="Update Product">
        </form>
    </div>
    
</body>
</html>